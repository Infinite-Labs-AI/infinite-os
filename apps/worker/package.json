{
  "name": "@infinite-os/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "dev": "tsx src/index.ts",
    "test": "vitest run --root ../.. ../../apps/worker/test/worker.test.ts",
    "typecheck": "tsc --noEmit --pretty false"
  },
  "dependencies": {
    "@infinite-os/analytical-engine": "workspace:*",
    "@infinite-os/config": "workspace:*",
    "@infinite-os/connectors": "workspace:*",
    "@infinite-os/db": "workspace:*",
    "@infinite-os/runtime": "workspace:*"
  }
}
