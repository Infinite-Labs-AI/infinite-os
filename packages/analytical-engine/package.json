{
  "name": "@infinite-os/analytical-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit --pretty false"
  },
  "dependencies": {
    "@infinite-os/core": "workspace:*",
    "@infinite-os/db": "workspace:*",
    "@infinite-os/connectors": "workspace:*",
    "@infinite-os/metadata": "workspace:*",
    "@infinite-os/runtime": "workspace:*"
  }
}
